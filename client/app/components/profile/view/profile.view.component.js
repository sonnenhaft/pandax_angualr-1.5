import template from './profile.view.html';
import controller from './profile.view.controller';

let profileViewComponent = {
  bindings: {},
  template,
  controller
};

export default profileViewComponent;
