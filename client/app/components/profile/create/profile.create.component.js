import template from './profile.create.html';
import controller from './profile.create.controller';

let profileCreateComponent = {
  template,
  controller
};

export default profileCreateComponent;
