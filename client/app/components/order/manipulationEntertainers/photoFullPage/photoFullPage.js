import angular from 'angular';
import photoFullPageComponent from './photoFullPage.component';

export default angular
  .module('photoFullPage', [

  ])
  .component('photoFullPage', photoFullPageComponent)
  .name;
