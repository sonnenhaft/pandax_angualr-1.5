import template from './main.html';
import controller from './main.controller';

let mainComponent = {
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default mainComponent;
