import template from './reset.html';
import controller from './reset.controller';

let resetComponent = {
  bindings: {
    output: '&'
  },
  template,
  controller
};

export default resetComponent;
